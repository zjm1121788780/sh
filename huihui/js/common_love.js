
	/*$("body").click();*/
	
 /*  $("body").click(function(){
      
        var contentext = '$wdE3YqkIFza$';      //复制内容（后台传过来的淘口令）
        $("body").attr("data-clipboard-text",contentext);
    });
    //触发方法
    var clipboard = new Clipboard('body');
    //回调函数（成功、失败）
    clipboard.on('success', function(e) {
        console.log(e);
    });
 
    clipboard.on('error', function(e) {
        console.log(e);
    });
*/